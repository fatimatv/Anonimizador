{
  "name": "@document-anonymizer/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "prisma generate --schema prisma/schema.prisma && tsc -p ../../packages/rules-engine/tsconfig.build.json && tsc -p tsconfig.build.json",
    "dev": "tsx watch src/main.ts",
    "lint": "eslint .",
    "prisma:generate": "prisma generate --schema prisma/schema.prisma",
    "prisma:migrate": "prisma migrate deploy --schema prisma/schema.prisma",
    "test": "prisma generate --schema prisma/schema.prisma && tsc -p ../../packages/rules-engine/tsconfig.build.json && vitest run",
    "typecheck": "prisma generate --schema prisma/schema.prisma && tsc -p ../../packages/rules-engine/tsconfig.build.json && tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@document-anonymizer/rules-engine": "workspace:*",
    "@fastify/cors": "^10.0.1",
    "@fastify/helmet": "^13.0.1",
    "@fastify/multipart": "^9.0.3",
    "@fastify/rate-limit": "^10.3.0",
    "@prisma/client": "^6.10.1",
    "argon2": "^0.43.0",
    "bullmq": "^5.56.0",
    "dotenv": "^16.5.0",
    "fastify": "^5.4.0",
    "ioredis": "^5.6.1",
    "mammoth": "^1.12.0",
    "pdf-parse": "^2.4.5",
    "zod": "^3.25.67"
  },
  "devDependencies": {
    "@types/node": "^22.15.34",
    "prisma": "^6.10.1",
    "tsx": "^4.20.3"
  }
}
